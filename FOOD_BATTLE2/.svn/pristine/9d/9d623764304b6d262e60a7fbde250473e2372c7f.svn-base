@charset "UTF-8";
@font-face{
font-family:dqfont;
src:url('PixelMplus10-Regular.woff')format('woff');
}
html{
	font-family:dqfont;
}
body{
	background-color: black;
	text: #ffff;
}
majin{
	margin-left:auto;
	margin-right:auto;
	left:auto;
	right:auto;
	text-left:auto;
	text-reight:auto;
}
/* --------------------------------------------- */
h1{	color:white;
    text-align: center;}
h3{	color:white;
    text-align: center;}
h5{color:white;
	text-alihn:center;}
/* --------------------------------------------- */
.picture{width:auto;
		 text-align: center;
		 margin-bottom:30px;
}
img2{
	width: 300x;
	height: 250px;
	position: center;
}
img{
	position: center;
}

/* --------------------------------------------- */
.comment{

    margin-left:auto;
	margin-right:auto;
	text-alihn:left;
	width:800px;
	height:200px;
	border-style:solid;
	border-color:white;
	border-radius:10px;
	color:white;
	padding: 10px;

}
/* --------------------------------------------- */
input{	 float:right;}
/* --------------------------------------------- */
button{
	font-family:dqfont;
	float:right;


}
button2 {
  display       : inline-block;
  border-radius : 19%;
  font-size     : 20pt;
  text-align    : center;
  cursor        : pointer;
  padding       : 15px 23px;
  background    : #000000;
  color         : #ffffff;
  line-height   : 1em;
  opacity       : 0.9;
  transition    : .3s;
  margin:auto;
  float:right;
}
button hover {
  opacity       : 1;
}
/*
button {
  display: inline-block;           改行なしのブロック要素
  position: relative;              相対位置指定
  width: 200px;                    横幅
  height: 60px;                    高さ
  text-align: center;              水平方向は中央揃え
  line-height: 56px;               行の高さ
}
button span {
  display: block;                  ブロック要素
  position: absolute;              親要素の左上を基準位置
  width: 100%;                     横幅
  height: 100%;                    高さ
  border: 2px solid #333;          罫線
  transform-style: preserve-3d;    重なりを3Dで表示
  transition: 2.2s;   変化時間
}
button span nth-child(1) {
  background-color: #000000;   Buttonボタンの色
  color: #ffffff;          Buttonの文字の色
  transform: rotateY(0deg);        縦軸の回転なし
  transform-origin: 50% 0 -100px;   transformの起点
}
button span nth-child(2) {
  background-color: rgba(0, 0, 0, 0.49);   Clickボタンの色
  color: #ffffff;          Clickの文字の色
  transform: rotateY(90deg);       縦軸で90度回転
  transform-origin: 50% 0 -100px;   transformの起点
}
button hover span nth-child(1) {
  transform: rotateY(-90deg);      縦軸で前方に90度回転
}
button hover span nth-child(2) {
  transform: rotateY(0deg);        縦軸の回転なし
} */
/* --- 背景が上下左右に動くのBOX定義 ------------------------ */
box {
  position   : relative;
  max-width  : 500px;
  height     : 200px;
  margin     : auto;
  background : #ffffff;
}

/* --- 背景が上下左右に動くの背景定義 ----------------------- */
box::before {
  display    : block;
  content    : "";
  position   : absolute;
  top        : 0;
  right      : 0;
  bottom     : 0;
  left       : 0;
  background-image : url(./image.jpg);
  animation  : bgAnime 21s linear infinite;
}
@keyframes bgAnime {
   0% { background-position: 0 0 }
 100% { background-position: 500px -332px }
}

/* --- 前面の文字定義（サンプルのため変更してください） ----- */
boxString{
  position   : absolute;
  display    : inline-block;
  padding    : 20px;
  background : rgba(0, 127, 255, 0.7);
  color      : #fff;
  top        : 50%;
  left       : 50%;
  transform  : translate(-50%,-50%);
}
table{ border=1;
	   align=center;
	    }